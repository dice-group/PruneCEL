SELECT ?class (MAX(?tp) AS ?posHits) (COUNT(?neg) AS ?negHits) WHERE {
    { SELECT ?class (COUNT(?pos) AS ?tp) WHERE {
        VALUES ?pos {  }
        POS_CONTEXT
    } GROUP BY ?class }
    VALUES ?neg { <https://github.com/qald_10#QUE134> <https://github.com/qald_10#QUE28> <https://github.com/qald_10#QUE128> }
    OPTIONAL {
        NEG_CONTEXT
    }
} GROUP BY ?class
ORDER BY DESC(?tp) (?tn)